{
  "name": "devtrail",
  "version": "0.1.2",
  "description": "DevTrail — a developer's trail on every Azure Boards work item.",
  "private": true,
  "scripts": {
    "build": "webpack --mode production",
    "dev": "webpack --mode development --watch",
    "package": "npm run build && tfx extension create --manifest-globs vss-extension.json --output-path out",
    "package:dev": "npm run build && tfx extension create --manifest-globs vss-extension.json --output-path out --rev-version"
  },
  "dependencies": {
    "azure-devops-extension-api": "^4.230.0",
    "azure-devops-extension-sdk": "^4.0.2",
    "dompurify": "^3.1.6",
    "marked": "^12.0.2",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "tslib": "^2.8.1"
  },
  "devDependencies": {
    "@types/react": "^17.0.80",
    "@types/react-dom": "^17.0.25",
    "copy-webpack-plugin": "^12.0.2",
    "css-loader": "^7.1.2",
    "style-loader": "^4.0.0",
    "tfx-cli": "^0.17.0",
    "ts-loader": "^9.5.1",
    "typescript": "^5.4.5",
    "webpack": "^5.91.0",
    "webpack-cli": "^5.1.4"
  }
}
